---
"gh-aw": patch
---

Aligned Copilot detection model default behavior with the main agent by removing the hardcoded fallback and using the same empty-string default path so the Copilot CLI native default applies when no model is configured.
