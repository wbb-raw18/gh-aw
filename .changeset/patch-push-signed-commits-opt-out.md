---
"gh-aw": patch
---

Allow `create-pull-request` and `push-to-pull-request-branch` to set `signed-commits: false` so repositories that do not require signed commits can push merge commits with direct `git push`.
