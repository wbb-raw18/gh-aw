---
"gh-aw": patch
---

Update `github.com/modelcontextprotocol/go-sdk` from v1.4.1 to v1.5.0. This update stabilizes client-side OAuth APIs, improves security by verifying `Origin` and `Content-Type` headers, fixes data races, and updates the protocol version to `2025-11-25`.
