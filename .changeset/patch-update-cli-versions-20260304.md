---
"gh-aw": patch
---

Bump the default Claude Code (2.1.66), Copilot CLI (0.0.421), and MCP Gateway (v0.1.8) versions so workflows get the latest bug fixes, Copilot's `--plugin-dir`, and the MCP Gateway HTTP session‑ID compliance improvement.
