---
"gh-aw": patch
---

Add `aw_context` caller metadata propagation for compiled `workflow_dispatch` workflows, so dispatch traceability data is automatically injected at trigger time and surfaced in `aw_info.json`.
