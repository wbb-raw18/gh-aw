---
"gh-aw": patch
---

Fix multi-repo checkout validation so the safe-output handlers use the normalized repo slug, the allowed-repos error path is tested, and slugs with extra slashes are rejected.
