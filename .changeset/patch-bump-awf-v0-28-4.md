---
"gh-aw": patch
---

Bump the default gh-aw-firewall version to v0.28.4 and refresh embedded container digest pins. v0.28.4 provides native AI-credits accounting for recognized dynamic model selectors (such as Copilot's `auto`), so no compiler-generated pricing overlay is required for zero-config `auto` runs with `maxAiCredits`.
