---
"gh-aw": patch
---

Fixed safe-output handler schemas and wiring so cross-repo properties (`target-repo`, `allowed-repos`) and handler auth config (including `github-token`) are available everywhere.
