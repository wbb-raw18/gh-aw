---
"gh-aw": patch
---

Detect and replace heredocs in generated workflow YAML by linting new shell heredoc sites and rendering safe-outputs files with a constrained JavaScript writer instead of shell evaluation.
