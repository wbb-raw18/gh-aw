---
"gh-aw": patch
---

Update the default Codex CLI to 0.107.0 and the MCP Gateway to v0.1.7 so workflows use the latest toolchain, including the new fork-thread branching helpers and guard_policies access control.
