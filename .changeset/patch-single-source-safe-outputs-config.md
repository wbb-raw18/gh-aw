---
"gh-aw": patch
---

Removed the duplicate safe outputs config generation path and now generate `config.json` from the handler registry as the single source of truth, ensuring handler fields stay consistent and complete.
