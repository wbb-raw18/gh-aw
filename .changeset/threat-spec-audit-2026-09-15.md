---
"gh-aw": patch
---

Daily compiler threat spec audit (2026-09-15): surveyed all open high-severity code-scanning alerts (0 critical). The result set is dominated by Opengrep build-reproducibility findings (`github-actions-npm-install-non-deterministic`, `actions-uv-pip-install-non-deterministic`, `actions-pip-install-inline-no-hash-check`, `github-actions-setup-node-missing-version`, `dockerfile-non-sha-pinned-image`) rather than compiler-generated-workflow threats. All compiler-generated `npm install` calls (`codex_engine.go`, `copilot_engine_installation.go`) already pin exact versions, satisfying the scanner's own documented exception; the remaining flagged items (`test-copilot-github-integration.yml`, `format-and-commit.yml`, the shared `uv pip install` snippet, and the repo `Dockerfile`) are hand-authored/build-tooling files outside `pkg/workflow/`/`pkg/parser/`/`actions/setup/` conformance scope, per specification Section 1's explicit exclusion of "external scanner ecosystems." No compiler/parser source diff exists beyond the single squashed commit state (`d8e1aa7`). No new `CTR-*` rule or implementation change was required; CTR-014 already covers the adjacent in-scope install-script-execution risk. No `threat-detection-suppress` annotations exist in any workflow. Bumped spec to 1.0.36.
