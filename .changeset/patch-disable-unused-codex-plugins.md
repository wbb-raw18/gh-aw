---
"gh-aw": patch
---

Disable the Codex plugin subsystem when a workflow declares no Agent Plugins, preventing unsupported ChatGPT plugin catalog and curated repository startup checks during API-key and BYOK runs.
