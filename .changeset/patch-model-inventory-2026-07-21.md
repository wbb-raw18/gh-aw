---
"gh-aw": patch
---

Model alias inventory update for 2026-07-21: add `claude-sonnet-5` patterns (`*sonnet-5*`) to the `sonnet-6x` alias so the `agent` alias can route to it, and add new `raptor-mini` alias for the Raptor mini model family.
