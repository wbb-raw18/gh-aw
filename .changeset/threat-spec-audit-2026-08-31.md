---
"gh-aw": patch
---

Daily compiler threat spec audit (2026-08-31): documented that the unified MCP config renderer's Playwright Chromium `--no-sandbox` entrypoint flag (`pkg/workflow/mcp_renderer_builtin.go`) is covered by the existing CTR-004 rationale; no new threat class or `CTR-*` rule required. Bumped spec to 1.0.29.
