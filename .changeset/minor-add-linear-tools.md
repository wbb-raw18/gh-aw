---
"gh-aw": minor
---

Add a first-class `tools.linear` integration for Linear's official hosted read-only MCP server, with a `LINEAR_API_KEY` secret default, configurable secret-safe bearer authentication, validated toolsets and tool filtering, and CLI-proxy support.
