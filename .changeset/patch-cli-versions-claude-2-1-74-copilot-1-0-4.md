---
"gh-aw": patch
---

Track CLI version updates: Claude Code 2.1.72→2.1.74 (adds `auto-mode` command) and Copilot CLI 1.0.3→1.0.4. Both tools use `"latest"` in constants.go, so no version-pinning changes are required.
