---
"gh-aw": patch
---

Stop exposing the legacy `cache.name` override so cache steps stay in sync with the schema and auto-generated names, and align the related docs/engines text plus the Gemini execution step label.
