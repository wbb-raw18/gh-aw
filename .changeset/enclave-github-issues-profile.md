---
"gh-aw": minor
---

Compile the opt-in `enclaves[].agent.github.cli: issues-read-v1` profile through
the shared mcpg gateway using distinct primary and enclave identities. Update
the minimum mcpg dependency to `v0.4.15`, which provides fail-closed per-agent
tool and repository policies.
