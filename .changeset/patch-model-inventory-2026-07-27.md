---
"gh-aw": patch
---

Model alias inventory update for 2026-07-27: add `claude-opus-5` ($5/1M in, $25/1M out, $0.5/1M cache read) to `github-copilot` provider in `models.json`; add `gemini-3.6-flash` version-specific alias for the Gemini 3.6 Flash family; update `agent` alias to include `gpt-5.5` and `gpt-5.6` after `gpt-5.4` in the fallback chain.
