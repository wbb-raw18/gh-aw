---
"gh-aw": patch
---

Add the `safe-outputs.activation-comments` flag so activation and fallback bot comments can be silenced and activation link messages now support templating and full XML markers.
