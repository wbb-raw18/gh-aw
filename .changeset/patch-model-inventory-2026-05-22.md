---
"gh-aw": patch
---

Model inventory update: remove `deprecated_models` metadata from multiplier payloads; keep complete model history by default; update daily model inventory guidance and AI Credits registry requirements to retain unseen models unless explicitly removed by maintainers.
