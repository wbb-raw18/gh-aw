---
"gh-aw": patch
---

Added support for trailing `/*` wildcards in `aw.yml` includes to select supported direct children of a directory, including source-to-destination mappings that install multiple files into `.github/workflows/`.
