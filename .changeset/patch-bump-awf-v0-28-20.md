---
"gh-aw": patch
---

Bump the default gh-aw-firewall version to `v0.28.20`, refresh the embedded AWF
config schema, and emit the native `enclaves[].agent.tools.github` AWF config
shape for static GitHub agent enclaves.
