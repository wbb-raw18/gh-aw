---
"gh-aw": minor
---

Surface grader results in `gh aw audit` and `gh aw logs`. Grader manifest and results are now mirrored into the compact `usage` artifact, a `graders` artifact set is available for artifact filtering, and grader outcomes appear in the audit console output, the audit JSON report, and per-run `gh aw logs --json` data.
