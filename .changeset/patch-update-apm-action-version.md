---
"gh-aw": patch
---

Updated `microsoft/apm-action` to `v1.4.1` and wired `DefaultAPMActionVersion` into APM dependency pin resolution to fix `GITHUB_TOKEN` handling for cross-org private repository installs.
