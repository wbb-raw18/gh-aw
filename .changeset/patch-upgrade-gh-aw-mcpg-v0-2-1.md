---
"gh-aw": patch
---

Upgrade `gh-aw-mcpg` to `v0.2.1`, updating the default MCP gateway image version and recompiling workflow lock files. This includes trusted bot detection improvements for `copilot-swe-agent` and commit GraphQL queries.
