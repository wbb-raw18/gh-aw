---
"gh-aw": minor
---

Add `safe-outputs.threat-detection.report-as-issue` option to disable the "[aw] Detection Runs" tracking issue/comment independently of threat detection. Defaults to `true`; set to `false` to keep detection and its `continue-on-error` enforcement active while skipping the tracking issue.
