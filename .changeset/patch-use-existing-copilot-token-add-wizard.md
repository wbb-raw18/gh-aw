---
"gh-aw": minor
---

Default add-wizard to use an existing `COPILOT_GITHUB_TOKEN` secret, reuse fetched repository metadata, make repository authoring support files optional, improve workflow and Copilot authentication prompt formatting, generate detailed pull request descriptions, and support `--gh-aw-ref` in both `add` and `add-wizard`.