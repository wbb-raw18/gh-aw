---
"gh-aw": patch
---

Upgrade `gh-aw-mcpg` to `v0.2.3`, updating the default MCP gateway image version and recompiling workflow lock files. This includes gateway bug fixes for integrity labeling, GraphQL bot-author filtering, and CLI startup validation.
