---
"gh-aw": patch
---

Move threat detection into a dedicated `detection` job for `safe-outputs` workflows, and make threat detection default to enabled unless `threat-detection: false` is set explicitly.
