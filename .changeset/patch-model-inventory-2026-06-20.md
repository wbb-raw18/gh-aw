---
"gh-aw": patch
---

Model alias inventory update for 2026-06-20: add `gpt-5.1` alias, add `image-generation` semantic alias for image-gen models across OpenAI and Gemini, update `vision` alias to include `google/` provider patterns, add `gpt-5.5` to the `agent` fallback chain before `gpt-5.4`, and add missing `gpt-image-2` and `gpt-5-search-api` entries to the model multiplier table.
