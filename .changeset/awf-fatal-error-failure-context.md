---
"gh-aw": patch
---

Surface AWF `[ERROR] Fatal error: ...` lines (such as a sandbox runtime preflight failure) in agent failure reports instead of misreporting them as a generic transient "engine terminated before producing output" failure.
