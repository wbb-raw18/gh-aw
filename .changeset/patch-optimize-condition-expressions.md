---
"gh-aw": patch
---

Add a boolean expression optimizer for workflow `if:` condition ASTs and apply it to compiler render paths to generate shorter equivalent expressions while preserving GitHub status function semantics.
