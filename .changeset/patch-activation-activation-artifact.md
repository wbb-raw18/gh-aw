---
"gh-aw": patch
---
Move the generated run info and workflow overview to the activation job, and upload them together as the "activation" artifact so downstream tooling can access the model info earlier.
