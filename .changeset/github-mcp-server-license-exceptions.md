---
"gh-aw": patch
---

Document license policy exceptions in `.grant.yaml` for the Debian base OS packages (`base-files`, `libc6`, `libssl3`, `media-types`, `netbase`, `tzdata`) shipped by the `ghcr.io/github/github-mcp-server` image, resolving the license policy violations reported by the container image security scan.
