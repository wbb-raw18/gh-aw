---
"gh-aw": patch
---

Merged firewall audit logs into the unified `agent` artifact for firewall-enabled workflows, and updated `gh aw logs`/`gh aw audit` artifact resolution to read firewall data from `agent` with backward-compatible fallbacks for legacy runs.
