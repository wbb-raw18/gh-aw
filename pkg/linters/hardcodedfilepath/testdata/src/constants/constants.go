// Package constants provides known file path constants for testing.
package constants

// ConfigFilePath is the path to the config file.
const ConfigFilePath = "/tmp/gh-aw/awf-config.json"

// ProxyLogsDir is the directory for proxy logs.
const ProxyLogsDir = "/tmp/gh-aw/sandbox/firewall/logs"

// AuditFilePath is the path to the audit file.
const AuditFilePath = "/tmp/gh-aw/pre-agent-audit.txt"
