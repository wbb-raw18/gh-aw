---
const { 
  href = "https://github.blog/ai-and-ml/automate-repository-tasks-with-github-agentic-workflows/",
  mainText = "Learn More on the GitHub Blog",
  subText = "Automate repository tasks with GitHub Agentic Workflows"
} = Astro.props;
---

<div class="prominent-link-wrapper">
  <a href={href} target="_blank" rel="noopener noreferrer" class="prominent-link-card">
    <span class="prominent-icon" aria-hidden="true">📖</span>
    <div class="prominent-text">
      <strong class="prominent-title">{mainText}</strong>
      <span class="prominent-subtitle">{subText}</span>
    </div>
    <span class="prominent-arrow" aria-hidden="true">→</span>
  </a>
</div>

<style>
  .prominent-link-wrapper {
    margin: 2.5rem auto;
    max-width: 720px;
    padding-inline: 1rem;
    text-align: center;
  }

  .prominent-link-wrapper .prominent-link-card {
    display: inline-flex;
    align-items: center;
    gap: 1.25rem;
    padding: 1.5rem 2rem;
    border: 1px solid #30363d;
    border-radius: 6px;
    background: rgba(13, 17, 23, 0.6);
    backdrop-filter: blur(8px);
    color: inherit;
    text-decoration: none;
    font-size: 1.1rem;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
    transition: all 0.2s ease;
    position: relative;
    overflow: hidden;
  }

  .prominent-link-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.03) 0%, transparent 100%);
    opacity: 0;
    transition: opacity 0.2s ease;
  }

  .prominent-link-card:hover::before {
    opacity: 1;
  }

  .prominent-link-wrapper .prominent-link-card:hover {
    border-color: #8b5cf6;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(139, 92, 246, 0.08);
  }

  .prominent-icon {
    font-size: 2rem;
    flex-shrink: 0;
    position: relative;
    z-index: 1;
  }

  .prominent-text {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
    text-align: left;
    position: relative;
    z-index: 1;
  }

  .prominent-title {
    display: block;
    font-size: 1.25rem;
    font-weight: 700;
    color: #f0f6fc;
    transition: all 0.2s ease;
  }

  .prominent-link-card:hover .prominent-title {
    color: #8b5cf6;
  }

  .prominent-subtitle {
    display: block;
    font-size: 0.95rem;
    color: #8d96a0;
    transition: color 0.2s ease;
  }

  .prominent-link-card:hover .prominent-subtitle {
    color: #a9b3be;
  }

  .prominent-arrow {
    font-size: 1.5rem;
    color: #8b5cf6;
    flex-shrink: 0;
    transition: transform 0.2s ease;
    position: relative;
    z-index: 1;
  }

  .prominent-link-card:hover .prominent-arrow {
    transform: translateX(5px);
  }

  /* Light theme support */
  :global([data-theme='light']) .prominent-link-card {
    background: rgba(255, 255, 255, 0.9);
    border-color: #d0d7de;
  }

  :global([data-theme='light']) .prominent-link-card::before {
    background: linear-gradient(135deg, rgba(130, 80, 223, 0.05) 0%, transparent 100%);
  }

  :global([data-theme='light']) .prominent-link-card:hover {
    border-color: #6639ba;
    box-shadow: 0 4px 12px rgba(102, 57, 186, 0.15);
  }

  :global([data-theme='light']) .prominent-title {
    color: #010409;
  }

  :global([data-theme='light']) .prominent-link-card:hover .prominent-title {
    color: #6639ba;
  }

  :global([data-theme='light']) .prominent-subtitle {
    color: #424a53;
  }

  :global([data-theme='light']) .prominent-link-card:hover .prominent-subtitle {
    color: #010409;
  }

  :global([data-theme='light']) .prominent-arrow {
    color: #6639ba;
  }

  @media (max-width: 640px) {
    .prominent-link-card {
      flex-direction: column;
      text-align: center;
      padding: 1.25rem;
      gap: 1rem;
    }

    .prominent-text {
      text-align: center;
    }

    .prominent-arrow {
      transform: rotate(90deg);
    }

    .prominent-link-card:hover .prominent-arrow {
      transform: rotate(90deg) translateX(5px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .prominent-link-card,
    .prominent-link-card::before,
    .prominent-title,
    .prominent-subtitle,
    .prominent-arrow {
      transition: none;
    }
  }
</style>
